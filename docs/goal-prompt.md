# Project goal prompt

Complete Longview for the Stockholm AI × Longevity Hackathon submission on Sunday 13 September 2026 at 12:00 Stockholm time. Deliver a deployed interactive demo, exactly three editable 16:9 PPTX slides plus PDF, a recorded fallback and a reproducible repository. Use the existing implementation, source register and measured evaluation; inspect their current state before changing them.

The intended user is a health ministry analyst. The country scope is Sweden, the United States and Singapore. The primary model outcome is life expectancy at birth, with a historically evaluated five-year forecast and a clearly exploratory ten-year scenario. Population pyramids provide observed demographic context. Policy findings give evidence context; they do not identify causal policy effects.

Keep every material finding linked to a dated primary source and a supporting passage. Use You.com and Tavily with the existing local credentials for discovery and cross-checking, within 500 combined attempted search/extraction requests including retries and the seven planning requests. Cache requests, preserve the ledger and purchase no credits. Never put keys, raw datasets or restricted/private records in Git or a public build. Check each dataset's source, licence, units, revision and missingness before use.

The initial evidence benchmark is frozen in `eval/benchmark.json`. Require actual human review of the four rubric checks; report the first completed verdict for every original record. The ≥90% requirement is visible on the page and must never be confused with forecast accuracy. Every publicly released finding additionally requires a current all-pass human verdict bound to its evidence version. Corrected errors remain in the original benchmark denominator. Supplemental findings also need approval before publication.

Maintain a wider training panel, temporal separation of labels and forecast origins, train-only preprocessing, and country holdouts for the three demo countries. Compare against no change and the previous trend. Report MAE in years, interval coverage and country-specific failures. Preserve the finding that the US regression loses to both baselines unless a new, justified evaluation changes that result. Do not improve metrics by changing the test protocol after seeing outcomes.

Investigate fundamental ageing research funding as a focused extension. Distinguish biological-mechanism research from dementia research, general health research, translation and eldercare. Keep programme budgets, annual outturns and multi-year commitments separate. Do not rank countries or fill unknown totals with zero before scope and fiscal periods match. This extension currently has two supplemental programme-level leads, not national totals.

Use React/TypeScript/Vite for the public UI and Python/SQLite for research and modelling. Public inference uses exported fitted parameters and a frozen evidence release. Host on Vercel or Netlify. Remote reviewers use the encrypted review packet and return exported verdict files for strict import; do not require localhost access. Verify the deployed user flow, deep links, mobile layout, source links, absence of credentials and read-only public behaviour.

Generate the deck from the same release as the app. Show the analyst's problem, actual demo, measured results, evidence-review state, limitations, architecture, data licences and team (Selja, Max, Jan). Keep the spoken pitch around three minutes until the organiser confirms the duration. No clinical claims or invented adoption benefits. Use the repository's avoid-ai-writing guidance for judge-facing writing.

Continue all work that does not depend on human input. Ask for the final missing review exports, persistent hosting claim or organiser access only when the concrete artifact is ready. Do not claim the project is complete while those submission requirements remain unresolved. In each change summary state judging alignment, verified behaviour, simulations/unverified claims and outstanding work, as required by AGENTS.md.
